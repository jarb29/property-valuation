services:
  flask-app:
    build:
      context: ..
      dockerfile: flask-app/Dockerfile
    ports:
      - "5002:5000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3